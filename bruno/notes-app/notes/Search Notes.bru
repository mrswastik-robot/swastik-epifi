meta {
  name: Search Notes
  type: http
  seq: 9
}

get {
  url: {{base_url}}/notes/search
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  q: first
  page: 1
  limit: 20
}

assert {
  res.status: eq 200
  res.body.data: isDefined
}
