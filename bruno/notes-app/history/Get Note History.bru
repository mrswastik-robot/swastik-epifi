meta {
  name: Get Note History
  type: http
  seq: 10
}

get {
  url: {{base_url}}/notes/:id/history
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: REPLACE_WITH_NOTE_ID
}

assert {
  res.status: eq 200
}
